# Policy-based

Learn the policy directly: $πθ(a∣s)$

The neural network directly outputs an action distribution or action.

Examples:

 - REINFORCE
 - Policy Gradient methods

Instead of asking:

How good is this action?

the algorithm learns:

What action should I take?